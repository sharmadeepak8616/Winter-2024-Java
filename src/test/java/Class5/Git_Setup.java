package Class5;

public class Git_Setup {
    public static void main(String[] args) {

        /**
         * Download and install git:
         *  use link -> https://git-scm.com/downloads
         *
         * Create account in github.com:
         * 1. Go to github.com
         * 2. If you have an account already, then login otherwise Create new account
         * 3. Verify your email address (if you need to)
         *
         * Connect your local(laptop/machine) with github account using ssh-key:
         * 1. open terminal(in Mac) or GitBash(in Windows)
         * 2. enter commands (pls update your email address before using command)
         *      ssh-keygen -t rsa -b 4096 -C "yourEmailAddress"
         * 3. enter command to see the generated ssh-key (public)
         *      cat .ssh/id_rsa.pub
         * 4. copy the ssh-key
         * 5. Go to Settings in Github account
         * 6. Go to Tab (on left-hand side) : SSH and GPG Key
         * 7. Click on "Add Ssh-key" button
         * 8. Provide name to your ssh-key
         * 9. paste the ssh-key in the given section
         * 10. Click on Add button
         *
         * Create repo in github account:
         * 1. Create a repository suitable to the project
         *
         * Clone created github-repo in local:
         * 1. Copy the link from github-repo
         * 2. open terminal(in Mac) or GitBash(in Windows)
         * 3. go to Desktop using cd-commands
         * 4. enter command
         *       git clone paste-the-github-clone-link
         *
         */





    }
}
