package Class6;

public class BasicGitCommands {
    public static void main(String[] args) {
        /**
         * Basic git commands:
         * git status
         *      shows the result after comparing project files in remote(cloud) vs local(laptop/machine)
         *
         * git add
         *      command to add files/folder to be sent to remote
         *
         * git commit
         *      command to write a short-message to mention why are we sending the files to remote
         *      git commit -m "short message"
         *
         * git push
         *      command to actually sent files (with commit message) in the remote
         *
         */
    }
}
